import streamlit as st
import pandas as pd
import datetime
import plotly.express as px

# 1. SETUP PAGE CONFIGURATION
st.set_page_config(page_title="Lubkung Education Director OS", layout="wide", page_icon="👑")

# แผงควบคุมระบบปฏิบัติการสำหรับผู้อำนวยการโรงเรียนบ้านหลุบกุง
st.title("👑 Lubkung Education Director OS")
st.subheader("ระบบปฏิบัติการดิจิทัลและแผงควบคุมการบริหารจัดการสถานศึกษา (สำหรับฝ่ายบริหาร)")
st.markdown("---")

# 2. DATABASE MOCK - ดึงข้อมูลความปลอดภัยภายนอกและการทำงานของครู
if 'school_status_db' not in st.session_state:
    st.session_state.school_status_db = pd.DataFrame([
        {"รายการตรวจ": "ระบบไฟฟ้าอาคารเรียน ป.1-ป.3", "กลุ่มงานผู้รับผิดชอบ": "กลุ่มงานบริหารทั่วไป", "สถานะความปลอดภัย": "🚨 ชำรุด/เสี่ยงภัยสูง", "ผู้รายงาน": "นายสิทธิพงษ์ แข็งขัน", "ข้อเสนอแนะเชิงนโยบาย": "รอเล่มแผนปฏิบัติการอนุมัติเพื่อตั้งเบิกงบจัดซื้อพัสดุสายไฟใหม่"},
        {"รายการตรวจ": "ระบบไฟฟ้าและพัดลมห้อง ป.6", "กลุ่มงานผู้รับผิดชอบ": "กลุ่มงานบริหารทั่วไป", "สถานะความปลอดภัย": "✅ ซ่อมบำรุงเร่งด่วนเรียบร้อย", "ผู้รายงาน": "นายสิทธิพงษ์ แข็งขัน", "ข้อเสนอแนะเชิงนโยบาย": "เปิดใช้งานได้ตามปกติ ปลอดภัยต่อเด็ก"},
        {"รายการตรวจ": "ความคืบหน้าเล่มแผนปฏิบัติการประจำปี", "กลุ่มงานผู้รับผิดชอบ": "กลุ่มงานบริหารวิชาการ", "สถานะความปลอดภัย": "⚠️ ล่าช้า (ยังไม่ส่งรูปเล่ม)", "ผู้รายงาน": "กลุ่มงานบริหารทั่วไป (ทักท้วง)", "ข้อเสนอแนะเชิงนโยบาย": "ผอ. ควรออกคำสั่งเร่งรัดในที่ประชุมด่วน"},
    ])

# 3. DIRECTORS NAVIGATION TABS
tab1, tab2, tab3 = st.tabs(["📊 1. หน้าจอตรวจราชการและระบบเกียร์ว่าง", "⚡ 2. ระบบสั่งการและอนุมัติพัสดุเร่งด่วน", "📝 3. สมุดบันทึกการออกไปราชการของ ผอ."])

# ==========================================
# TAB 1: หน้าจอตรวจราชการและระบบเกียร์ว่าง (Director Real-time Monitor)
# ==========================================
with tab1:
    st.header("🔍 สรุปสถิติการปฏิบัติงานและจุดเสี่ยงภัยในโรงเรียน")
    st.info("💡 ส่วนนี้ดึงข้อมูลมาจากระบบ EveryTime Tracker ของครู เพื่อให้ ผอ. นั่งตรวจบนมือถือได้ว่า ใครมาสาย ใครออกก่อนเวลา หรือกลุ่มงานไหนปล่อยเกียร์ว่างตอน ผอ. ไม่อยู่")
    
    col1, col2, col3 = st.columns(3)
    col1.metric("🔴 จุดเสี่ยงภัย/ไฟฟ้าชำรุดที่ต้องอนุมัติซ่อม", "1 จุดเสี่ยงสูง")
    col2.metric("⏳ โครงสร้างโครงการที่ยังล่าช้า", "แผนปฏิบัติการปี 2569")
    col3.metric("📉 อัตราภาวะเกียร์ว่างเฉลี่ย (สัปดาห์นี้)", "65% (ช่วงบ่ายหลัง ผอ. ไปธุระ)")
    
    st.markdown("### 📋 ตารางแจ้งเตือนสถานะเชิงโครงสร้างของโรงเรียนบ้านหลุบกุง")
    st.dataframe(st.session_state.school_status_db, use_container_width=True)

# ==========================================
# TAB 2: ระบบสั่งการและอนุมัติพัสดุเร่งด่วน (Executive Order)
# ==========================================
with tab2:
    st.header("⚡ ระบบข้อบัญชาการและการอนุมัติพัสดุเชิงรุก")
    st.markdown("ผอ. สามารถกดปุ่มสั่งการข้ามจังหวัดผ่านมือถือ เพื่อบังคับใช้กฎหมายและระเบียบพัสดุได้ทันที")
    
    order_title = st.selectbox("เลือกประเด็นนโยบายที่ต้องการสั่งการเร่งด่วน", [
        "ออกคำสั่งเร่งรัดกลุ่มงานวิชาการให้นำส่งกรอบโครงการภายใน 3 วัน",
        "อนุมัติงบประมาณสำรองจ่ายเพื่อจัดซื้อสายไฟและอุปกรณ์พัดลมทดแทนจุดเสี่ยงภัย",
        "คาดโทษและหักคะแนนความประพฤติบุคลากรที่ละทิ้งหน้าที่ก่อนเวลา 16.30 น.",
        "ปรับตารางเวลาลดความตึงเครียดนักเรียน (เน้นเต็มเวลา 08.30 - 16.30 น.)"
    ])
    
    commander_note = st.text_area("ข้อความสั่งการเพิ่มเติมจาก ผอ. (จะแจ้งเตือนไปยังกลุ่มงานที่เกี่ยวข้อง)", placeholder="พิมพ์กำชับ เช่น ให้รายงานผลให้ทราบภายในวันพรุ่งนี้...")
    
    if st.button("🚀 สั่งการลงระบบราชการดิจิทัล (Director Command)"):
        st.success(f"ระบบได้บันทึกคำสั่ง: '{order_title}' ลงฐานข้อมูลกลางแล้ว คำสั่งนี้มีผลผูกพันทางกฎหมาย ข้าราชการครูที่หลบเลี่ยงจะถือว่าขัดคำสั่งผู้บังคับบัญชา")

# ==========================================
# TAB 3: สมุดบันทึกการออกไปราชการของ ผอ. (Director Transparency Log)
# ==========================================
with tab3:
    st.header("📝 บันทึกภารกิจและการไปราชการ/ธุระ ของฝ่ายบริหาร")
    st.markdown("เพื่อความโปร่งใสตามหลักธรรมาภิบาล ผอ. สามารถลงบันทึกเวลาออกไปราชการ เพื่อตัดข้อครหา และใช้คุมระบบควบคุมภายใน")
    
    d_col1, d_col2 = st.columns(2)
    with d_col1:
        go_date = st.date_input("วันที่เดินทางออกนอกสถานศึกษา", datetime.date.today())
        reason = st.text_input("ไปราชการ/ธุระ เรื่องใด", placeholder="เช่น ประชุมหัวหน้าส่วนราชการ ณ สพป.นม. เขต 6")
    with d_col2:
        time_leave = st.time_input("เวลาที่เดินทางออกจากโรงเรียน", datetime.time(13, 0))
        assign_user = st.text_input("ผู้รักษาราชการแทน (กรณีไม่อยู่ปฏิบัติหน้าที่)", value="นายสิทธิพงษ์ แข็งขัน (หัวหน้างานบริหารทั่วไป)")
        
    if st.button("💾 บันทึกเวลาลงเวลาทำงานฝ่ายบริหาร"):
        st.success("บันทึกตารางงาน ผอ. เรียบร้อย ระบบจะซิงค์เวลาเข้ามือถือครูทุกคนเพื่อรับทราบผู้รักษาการแทน")